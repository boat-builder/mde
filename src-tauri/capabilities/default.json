{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window and spawned editor windows",
  "windows": ["main", "win-*"],
  "permissions": [
    "core:default",
    "dialog:default"
  ]
}
